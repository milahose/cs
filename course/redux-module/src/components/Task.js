import React, { PropTypes } from 'react';

const Task = (props) => {
  return (
    <article>
      <button></button>
    </article>
  );
};

Task.propTypes = {
  task: PropTypes.object,
};

export default Task;
